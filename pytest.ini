[pytest]
asyncio_mode = auto
testpaths = tests
# Needed so `custom_components.wakey` imports under a bare `pytest` invocation.
# `python -m pytest` happens to work without it because that form puts the
# working directory on sys.path; CI calls `pytest` directly and does not.
pythonpath = .
